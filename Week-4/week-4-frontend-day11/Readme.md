// Fronted basics
--> HTML is a document // markup language not a programming language
--> To access the html document to ease 1) type html go with html:5 and enter
                                        2) press shift with! and press enter you will get basic html document 
--> HTML tag contains mostly head and body elements
                        -->in head contains two meta
                             1)---> charset 
                             2)meta-->responsive page 
--> Html never focus on presentation
--> The reason behind every browser will contain default style sheet user  stylesheet
--> For every browser will have diff style sheet 
--> when the link in blue it is not visited if the link  is in red it is appears in red
--> if the tag has some data it will have close tag 
--> Why heading  elements are going next line because these are block level ele
--> Img and links are inline ele thats why it uses only how much they occupies space 


---          CSS
-->Css contains rules
In css file h1 is a selector 
--> In css to write easy code everyone must understand the box model
--> In css pov every html ele has the box 
--> Every ele has contain the border
-->The space between content and border is called as padding.[it has four sides]
-->we can change the space using padding in css
-->The space outside of the border is called margin [it has four sides]
--> To alignment an ele we align left right and center
--> inline can block the ele
-->margin as auto 